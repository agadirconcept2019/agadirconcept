# media

Sprint 1 media management foundation:
- media metadata model is defined in `admin/schemas/content-types.yaml`
- admin list/form behavior is defined in `admin/schemas/collection-views.yaml`
- storage/repository contract is defined in `persistence/schemas/cms-repositories.yaml`

Implementation of upload pipeline/security checks will be added in a later validated sprint.
